import React, { useState } from "react";

const HomePage = () => {

    return (
        <div>
            <div>
             hello, there are the main page with the calendar, i will plan the design next and then returns to it!
                
            </div>
        </div>
    );
};

export default HomePage;
